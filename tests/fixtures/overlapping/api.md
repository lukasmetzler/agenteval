# API Guidelines

Use TypeScript strict mode for all API code. Prefer const over let for variable declarations. Use early returns for cleaner control flow in functions. Always write unit tests for new API functions. Handle errors explicitly with try-catch blocks.

## Endpoints

Use REST conventions. Validate input with Zod schemas.
