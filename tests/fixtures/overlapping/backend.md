# Backend Guidelines

Use TypeScript strict mode for all backend code. Prefer const over let for variable declarations. Use early returns for cleaner control flow in functions. Always write unit tests for new backend functions. Handle errors explicitly with try-catch blocks.

## Database

Use PostgreSQL with connection pooling. Run migrations before every deploy.
