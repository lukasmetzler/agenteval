---
name: example-project
description: A well-structured instruction file for testing.
---

# Project Instructions

## Code Style

Use TypeScript strict mode. Prefer `const` over `let`. Use early returns.

## Testing

Run tests with `bun test`. All new code needs unit tests.

## Git

Feature branches: `feat/<name>`. Squash-merge to main.
